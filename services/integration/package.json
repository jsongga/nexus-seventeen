{
  "name": "@cicada/steward-integration",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Deterministic HTTP vertical-slice tests for Steward",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test dist/test/*.test.js"
  },
  "dependencies": {
    "@cicada/steward-control-plane": "0.1.0",
    "@cicada/steward-deployment-broker": "0.3.0",
    "@cicada/steward-manager-review": "0.1.0",
    "@cicada/steward-protocol": "0.1.0",
    "@cicada/steward-supervisor": "0.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.19.21",
    "typescript": "^5.7.3"
  },
  "engines": {
    "node": ">=22"
  }
}
