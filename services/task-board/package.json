{
  "name": "@cicada/steward-task-board",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Authoritative SQLite task board and event-gated agent wakeup service for Steward",
  "exports": {
    ".": {
      "types": "./dist/src/index.d.ts",
      "default": "./dist/src/index.js"
    }
  },
  "engines": {
    "node": ">=22.13.0 <23 || >=24.0.0"
  },
  "scripts": {
    "build": "npm run build -w @cicada/steward-task-board-contract && tsc -p tsconfig.json",
    "typecheck": "npm run build -w @cicada/steward-task-board-contract && tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test dist/test/*.test.js",
    "start": "node dist/src/main.js"
  },
  "dependencies": {
    "@cicada/steward-task-board-contract": "0.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.19.21",
    "typescript": "^5.7.3"
  }
}
