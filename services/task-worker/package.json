{
  "name": "@cicada/steward-task-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Event-driven one-shot agent executor for the durable Steward task board",
  "exports": {
    ".": {
      "types": "./dist/src/index.d.ts",
      "default": "./dist/src/index.js"
    }
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "build": "npm run build -w @cicada/steward-task-board && tsc -p tsconfig.json",
    "typecheck": "npm run build -w @cicada/steward-task-board && tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test dist/test/*.test.js",
    "start": "node dist/src/main.js"
  },
  "dependencies": {
    "@cicada/steward-task-board-contract": "0.1.0"
  },
  "devDependencies": {
    "@cicada/steward-task-board": "0.1.0",
    "@types/node": "^22.19.21",
    "typescript": "^5.7.3"
  }
}
