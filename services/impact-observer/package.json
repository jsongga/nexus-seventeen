{
  "name": "@cicada/steward-impact-observer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Read-only, bounded user-impact summaries for Steward tasks",
  "exports": {
    ".": {
      "types": "./dist/src/index.d.ts",
      "default": "./dist/src/index.js"
    }
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test dist/test/*.test.js",
    "start": "node dist/src/main.js"
  },
  "dependencies": {
    "@cicada/steward-model-routing": "0.1.0",
    "@cicada/steward-protocol": "0.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.19.21",
    "typescript": "^5.7.3"
  }
}
